#!/usr/bin/env sh
#
# Minimal Gradle wrapper script placeholder.
# If Android Studio complains about missing wrapper, regenerate wrapper from AS or download wrapper jar as described in README.

DIR="$(cd "$(dirname "$0")" && pwd)"
java -jar "$DIR/gradle/wrapper/gradle-wrapper.jar" "$@"

